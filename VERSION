# Nous Agent Runner version manifest (source of truth for packaging)

# Main product version (also used as the default service image tag).
NOUS_AGENT_RUNNER_VERSION=0.2.10

# VM OS image version used by offline assets (arm64).
# This must match the qcow2 filename downloaded by `scripts/macos/fetch_offline_assets.sh`.
NOUS_VM_VERSION=debian-12-genericcloud-arm64-20251112-2294.qcow2
